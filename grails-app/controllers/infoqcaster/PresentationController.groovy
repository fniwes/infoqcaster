package infoqcaster
class PresentationController {

    def index() {
        // Grails 3 requires a controller to return a map to render the view.
        // otherwise it just return a empty response
        return [:]
    }
}
